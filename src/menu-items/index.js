// project import

import dashboard from "./dashboard";
import utilities from "./utilities";
import clients from "./clients";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, clients, utilities],
  // items: [dashboard, pages, utilities, clients]
};

export default menuItems;
